# Streamlit OCR Photo Calculator

This project is a Streamlit web app that reads numbers from a photo using OCR
and calculates the total automatically.

## Features
- Upload a photo of numbers or a bill
- Extract numbers using Tesseract OCR
- Automatically calculate the total

## Run locally

pip install -r requirements.txt
streamlit run app.py